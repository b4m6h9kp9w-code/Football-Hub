export function renderFormations(container, { db, AppState }) {
  container.innerHTML = `
    <div class="coming-soon">
      <div class="coming-soon__icon">⬛</div>
      <h2>Formations</h2>
      <p>Coming in Phase 2 — Formation builder will launch alongside the Play Designer.</p>
    </div>
  `;
}
