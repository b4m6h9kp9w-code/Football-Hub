export function renderPlayDesigner(container, { db, AppState }) {
  container.innerHTML = `
    <div class="coming-soon">
      <div class="coming-soon__icon">✏️</div>
      <h2>Play Designer</h2>
      <p>Coming in Phase 2 — This will be the full interactive play drawing canvas.</p>
    </div>
  `;
}
