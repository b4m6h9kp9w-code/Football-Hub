export function renderOpponents(container, { db, AppState }) {
  container.innerHTML = `
    <div class="coming-soon">
      <div class="coming-soon__icon">🎯</div>
      <h2>Opponents</h2>
      <p>Coming in Phase 3 — Track opponent tendencies and associate plays.</p>
    </div>
  `;
}
